Utils = {
    screen: {
        width: function() {
            return window.innerWidth || $(window).width();
        },
        
        height: function() {
            return window.innerHeight || $(window).height();
        },
    }
}